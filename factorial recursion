 Write a go program to calculate factorial using recursion

	// recursion

	package main
	import "fmt"
	func factorial(n int) int{
	   if n == 1 || n == 0{
		  return n
	   }
	   return n * factorial(n-1)
	}
	func main(){
	   fmt.Println(factorial(7))

	}
